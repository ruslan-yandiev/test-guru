<tr>
  <td><%= answer.body %></td>
  <td><%= link_to t('links.show'), admin_answer_path(answer), class: 'btn btn-primary' %></td>
  <td><%= link_to t('links.edit'), edit_admin_answer_path(answer), class: 'btn btn-primary' %></td>
  <td><%= link_to t('links.delete.title'), admin_answer_path(answer), method: :delete, data: { confirm: t('links.delete.confirm') }, class: 'btn btn-danger' %></td>
</tr>